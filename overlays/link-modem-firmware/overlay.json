{
  "name": "link-modem-firmware",
  "description": "Expose the modem firmware (core_nhlos_a, mounted at /vendor/modem) under the path the kernel's request_firmware() searches, fixing the DSP/modem remoteproc 'request_firmware failed: -2' at boot. Applies to the current layout, where the modem firmware lives on core_nhlos_a.",
  "commands": [
    {
      "type": "chroot-cmd",
      "cmd": "mkdir -p /lib/firmware/tachyon && ln -sf /vendor/modem /lib/firmware/tachyon/modem"
    }
  ]
}
